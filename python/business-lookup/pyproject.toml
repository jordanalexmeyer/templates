[project]
name = "business-lookup"
version = "0.1.0"
description = "Retrieve an official SF business record with Browserbase Fetch API"
readme = "README.md"
requires-python = ">=3.11,<3.14"
dependencies = [
    "browserbase>=1.7.0",
    "pydantic>=2.12,<3",
    "python-dotenv==1.2.2",
]

[tool.uv]
package = false
