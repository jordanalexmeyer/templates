[project]
name = "sec-filing-research"
version = "0.1.0"
description = "Retrieve SEC filing metadata with Browserbase Fetch API"
readme = "README.md"
requires-python = ">=3.11,<3.14"
dependencies = [
    "browserbase>=1.7.0",
    "python-dotenv==1.2.2",
]

[tool.uv]
package = false
